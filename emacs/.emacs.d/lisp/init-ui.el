; (use-package emacs
;              :unless *is-windows*
;              :config
;              (setq display-numbers-type 'relative)
;              (global-display-line-numbers-mode t))

(setq display-numbers-type 'relative)
(global-display-line-numbers-mode t)

(provide 'init-ui)
