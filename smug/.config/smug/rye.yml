session: rye-shell

root: /home/se7en/PyProject/se7en-rye

# before_start:
#   - docker-compose -f my-microservices/docker-compose.yml up -d # my-microservices/docker-compose.yml is a relative to `root`

# env:
#   FOO: BAR

# stop:
#   - docker stop $(docker ps -q)

windows:
  - name: env
    # layout: tiled  #  NOTE: layout: tiled, main-vertical, main-horizontal
    commands:
      - clear
      - rye shell
      - lf
    panes:
      - type: horizontal #  NOTE: horizontal
        commands:
          - clear
          - rye shell
          - cd ./movies
          - clear
